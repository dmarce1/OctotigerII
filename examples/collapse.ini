problem.name=collapse
# 3D gas plus isolated self-gravity; conservative gravitational energy by default
# All dimensional values are cgs.
mesh.cells=4
mesh.level=1
runtime.stopTime=5
output.directory=output/collapse
output.every=1
