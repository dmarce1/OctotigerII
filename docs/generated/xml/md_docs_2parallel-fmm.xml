<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.9.8" xml:lang="en-US">
  <compounddef id="md_docs_2parallel-fmm" kind="page">
    <compoundname>md_docs_2parallel-fmm</compoundname>
    <title>Threaded and distributed FMM</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><anchor id="md_docs_2parallel-fmm_1autotoc_md0"/> </para>
<sect1 id="md_docs_2parallel-fmm_1autotoc_md1">
<title>Execution and ownership</title>
<para>The application calls <computeroutput>Runtime::solveGravity()</computeroutput>. A persistent <computeroutput>gravity::FieldSolver</computeroutput> creates one <computeroutput>FmmPartition</computeroutput> component on every HPX locality. At each cell-octree level, the global x-contiguous cell indices are divided into nearly equal, contiguous ranges. Each locality stores only its owned moments and local expansions. Empty ranges at coarse levels are supported. Leaf moments contain only mass; higher levels contain the existing <computeroutput>(p+1)^2</computeroutput> coefficients.</para>
<para>This partition is independent of the field-block placement. Contiguous leaf segments are mapped to field ranges once. Density is fetched directly from those ranges, including remote ranges when an FMM partition cuts a field block. There is no full-density gather or replicated global hierarchy. Small coarse levels can naturally be needed by several localities.</para>
<para>Within a locality, bounded HPX tasks claim batches of 16 targets. By default the number of tasks is bounded by <computeroutput>hpx::get_os_thread_count()</computeroutput>; <computeroutput>runtime.workerTasks</computeroutput> can select another bound. No per-cell HPX task is created. One target expansion has exactly one writer. Static FMM ownership does not use transport work stealing.</para>
</sect1>
<sect1 id="md_docs_2parallel-fmm_1autotoc_md2">
<title>Stages</title>
<para><orderedlist>
<listitem><para>Clear the local hierarchy and read masses from the published field bank.</para>
</listitem><listitem><para>Traverse upward, level by level. Owners fetch required child moments and independently translate and sum eight children in fixed slot order.</para>
</listitem><listitem><para>Traverse downward, level by level. Owners fetch the same-level source moments needed for M2L (or leaf masses for P2P), and their parent local expansions. Each target sums its interactions and then its translated parent local.</para>
</listitem><listitem><para>Evaluate potential and acceleration and write the other field bank. Copy the other enabled state fields into that bank without modifying them.</para>
</listitem><listitem><para>Publish the new bank and generation after every locality has completed.</para>
</listitem></orderedlist>
</para>
<para>Each level has a completion barrier. Source requests are deduplicated by owner and split into bounded parcels; independent reads launch before waiting. Local sources are referenced directly, and remote halos are discarded after the stage. The persistent hierarchy is reused at the next solve.</para>
<para>All outstanding tasks and transfers are drained before an ordinary numerical or remote-action exception escapes. The runtime does not change its published bank, generation, gravity time, or physical time on a failed solve. A later solve clears the hierarchy before retrying. As with the existing runtime, process loss and out-of-memory recovery are not fault-tolerant execution modes.</para>
</sect1>
<sect1 id="md_docs_2parallel-fmm_1autotoc_md3">
<title>Interaction equivalence</title>
<para>The opening condition is still <computeroutput>distance * theta &gt; 1</computeroutput> in cell-width units. For a target at a given level, candidates are the children of source parents whose separation from the target parent fails that opening condition. A nonleaf pair is accepted if its current separation passes. Leaves evaluate all remaining candidates directly, excluding self terms.</para>
<para>For <computeroutput>theta &lt; 1/sqrt(3)</computeroutput>, descendants of an accepted pair remain separated: <computeroutput>d_child &gt;= 2*d_parent - sqrt(3) &gt; 1/theta</computeroutput>. Consequently an unaccepted parent also has unaccepted ancestors, and this interaction list reproduces the original recursive pair traversal. The test suite checks identical unordered pair counts.</para>
<para>The original traversal updates both targets of a pair together. The parallel solver evaluates the two directed contributions on their respective target owners. This performs the same two translations/direct contributions and requires no atomics on coefficients. Pair statistics count only <computeroutput>target &lt; source</computeroutput>, preserving their previous meaning. Accumulation order can differ from the old solver. Within the new solver it is independent of thread and locality count.</para>
<para><computeroutput>gravity::solve</computeroutput> remains the standalone serial reference used by numerical tests. The application uses <computeroutput>FieldSolver</computeroutput> in both HPX and serial builds; an HPX-free build executes the same target traversal sequentially.</para>
</sect1>
<sect1 id="md_docs_2parallel-fmm_1autotoc_md4">
<title>Running and testing</title>
<para>For an HPX build, append <computeroutput>--hpx:threads=12</computeroutput> to the existing problem command. The same executable must be launched on every locality using the installed HPX launcher and parcelport. The existing <computeroutput>tests/distributed.py</computeroutput> is a local-host TCP correctness launcher; it is not a cluster submission script.</para>
<para>From HOME, replacing the project path if needed:</para>
<para><programlisting filename=".bash"><codeline><highlight class="normal">ctest<sp/>--test-dir<sp/>~/workspace/OctotigerII/release/gravity-sphere/3d<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>--output-on-failure<sp/>-R<sp/>&apos;gravity\.(partitioned|distributed)&apos;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">OCTOTIGERII_TEST_LOCALITIES=3<sp/>OCTOTIGERII_TEST_THREADS=2<sp/>\</highlight></codeline>
<codeline><highlight class="normal">python3<sp/>~/workspace/OctotigerII/tests/distributed.py<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>~/workspace/OctotigerII/release/gravity-sphere/3d/tests/gravityParallelChecks</highlight></codeline>
</programlisting></para>
<para>The console&apos;s <computeroutput>cellsPerLocality</computeroutput> reports the number of leaves evaluated on each locality in the last solve. <computeroutput>workerTasks</computeroutput> counts bounded tasks dispatched across all FMM stages, including exchange planning; it is not a timing or utilization measurement. See <computeroutput><ref refid="VALIDATION_8md" kindref="compound">VALIDATION.md</ref></computeroutput> for measured test results.</para>
</sect1>
<sect1 id="md_docs_2parallel-fmm_1autotoc_md5">
<title>Remaining limits</title>
<para>Tree-level barriers and sparse coarse levels limit strong scaling. Ownership is static and balanced by cell count rather than measured interaction cost. Geometry metadata is still replicated by the existing runtime. Snapshot collection, diagnostics, Silo output, and the sampled direct-reference calculation remain centralized and can limit full-application scaling. No GPU path is introduced. </para>
</sect1>
    </detaileddescription>
    <location file="docs/parallel-fmm.md"/>
  </compounddef>
</doxygen>
