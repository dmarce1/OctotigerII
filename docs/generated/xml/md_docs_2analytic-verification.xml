<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.9.8" xml:lang="en-US">
  <compounddef id="md_docs_2analytic-verification" kind="page">
    <compoundname>md_docs_2analytic-verification</compoundname>
    <title>Analytic verification</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><anchor id="md_docs_2analytic-verification_1autotoc_md6"/> Every problem implements <computeroutput>problemReference(Config const&amp;)</computeroutput> alongside its initial conditions. Analytic comparison is <bold>automatic by default</bold> when a reference is available. For gravity builds, the default reference is instead the direct sum of the current discrete mass distribution, including for evolving collapse. This checks gravity fields only. A non-gravity problem without a continuum reference reports <computeroutput>unavailable</computeroutput>, with a reason. An unavailable reference is never reported as a passed accuracy test.</para>
<sect1 id="md_docs_2analytic-verification_1autotoc_md7">
<title>Available references</title>
<para><table rows="7" cols="3"><row>
<entry thead="yes"><para>Problem   </para>
</entry><entry thead="yes"><para>Dimensions   </para>
</entry><entry thead="yes"><para>Exact solution and restrictions    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>sod</computeroutput>   </para>
</entry><entry thead="no"><para>1, 2, 3   </para>
</entry><entry thead="no"><para>Ideal-gas Riemann solution for the configured <computeroutput>hydro.gamma</computeroutput>; valid until the first wave reaches an outflow boundary.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>gravity-sphere</computeroutput>   </para>
</entry><entry thead="no"><para>3   </para>
</entry><entry thead="no"><para>Direct discrete reference by default; optional continuum isolated uniform sphere, including interior and exterior potential and acceleration; potential vanishes at infinity.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>gravity-gaussian</computeroutput>   </para>
</entry><entry thead="no"><para>3   </para>
</entry><entry thead="no"><para>Direct discrete reference by default; optional continuum isolated spherical Gaussian truncated at radius half the domain width, including the contribution of exterior shells to the interior potential.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>streaming</computeroutput>   </para>
</entry><entry thead="no"><para>1, 2, 3   </para>
</entry><entry thead="no"><para>Periodic translation along the diagonal of the active dimensions, with transport speed <computeroutput>lightSpeedRatio*c</computeroutput> and physical flux magnitude <computeroutput>c*E</computeroutput>.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>collapse</computeroutput>   </para>
</entry><entry thead="no"><para>3   </para>
</entry><entry thead="no"><para>Direct gravity reference at the current time; no analytic hydrodynamic solution.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>kelvin-helmholtz</computeroutput>, <computeroutput>radiation-pulse</computeroutput>   </para>
</entry><entry thead="no"><para>As supported by their manifests   </para>
</entry><entry thead="no"><para>No full analytic reference currently implemented; status is explicitly unavailable.   </para>
</entry></row>
</table>
</para>
<para>The Gaussian benchmark now has a <bold>spherical cutoff</bold>, rather than the old implicit cubical truncation. Its center density remains <computeroutput>1e4 g/cm^3</computeroutput>, its standard deviation is <computeroutput>0.15</computeroutput> times the domain width, and its cutoff radius is <computeroutput>0.5</computeroutput> times that width. Initial conditions and the reference use the same cutoff. This is a deliberate change to this benchmark&apos;s density distribution.</para>
</sect1>
<sect1 id="md_docs_2analytic-verification_1autotoc_md8">
<title>Running a comparison</title>
<para>Use the executable for the chosen problem and dimension. For example, starting from HOME, with the checkout at <computeroutput>~/workspace/OctotigerII</computeroutput>:</para>
<para><programlisting filename=".bash"><codeline><highlight class="normal">cd<sp/>&quot;$HOME/workspace/OctotigerII&quot;</highlight></codeline>
<codeline><highlight class="normal">cmake<sp/>-S<sp/>.<sp/>-B<sp/>release/sod-1d<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>-DCMAKE_BUILD_TYPE=Release<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>-DOCTOTIGERII_PROBLEM=sod<sp/>-DOCTOTIGERII_NDIM=1</highlight></codeline>
<codeline><highlight class="normal">cmake<sp/>--build<sp/>release/sod-1d<sp/>-j</highlight></codeline>
<codeline><highlight class="normal">./release/sod-1d/octotigerII-sod-1d<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>--config=bin/hydro_tests/Sod/inputs<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>--verification.analytic=on<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>--mesh.cells=32<sp/>--mesh.level=2<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>--output.directory=output/sod-analytic</highlight></codeline>
<codeline><highlight class="normal">ctest<sp/>--test-dir<sp/>release/sod-1d<sp/>--output-on-failure</highlight></codeline>
</programlisting></para>
<para>Supply the normal HPX dependency paths for your installation, or configure with <computeroutput>-DOCTOTIGERII_WITH_HPX=OFF</computeroutput> for the serial backend. Gravity builds use <computeroutput>-DOCTOTIGERII_PROBLEM=gravity-sphere</computeroutput> or <computeroutput>gravity-gaussian</computeroutput>, and dimension 3. Their input files are <computeroutput>bin/gravity_tests/Sphere/inputs</computeroutput> and <computeroutput>bin/gravity_tests/Gaussian/inputs</computeroutput>. Gravity tests require <computeroutput>runtime.stopTime=0</computeroutput>.</para>
<para><table rows="4" cols="3"><row>
<entry thead="yes"><para>Option   </para>
</entry><entry thead="yes"><para>Default   </para>
</entry><entry thead="yes"><para>Meaning    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>verification.analytic</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>auto</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>auto</computeroutput>: compare when available; <computeroutput>on</computeroutput>: require a valid reference; <computeroutput>off</computeroutput>: disable.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>verification.relativeL1Tolerance</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>-1</computeroutput>   </para>
</entry><entry thead="no"><para>A nonnegative value requires every nonzero-reference field&apos;s relative L1 error to be at most this value. Failure exits nonzero. <computeroutput>-1</computeroutput> disables the gate.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>verification.absoluteTolerance</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>1e-12</computeroutput>   </para>
</entry><entry thead="no"><para>Maximum absolute Linf for identically zero reference fields when the gate is enabled, in each field&apos;s CGS units.   </para>
</entry></row>
</table>
</para>
<para>These options support both dotted INI keys and <computeroutput>[verification]</computeroutput> sections. Tolerances are opt-in: the resolution, problem, and desired accuracy determine appropriate values. Reporting runs automatically without an accuracy gate. A gate requires an available reference and cannot be combined with <computeroutput>off</computeroutput>.</para>
</sect1>
<sect1 id="md_docs_2analytic-verification_1autotoc_md9">
<title>Output and norms</title>
<para>The final console summary lists each field&apos;s <bold>relative</bold> L1, L2, and Linf error. With a continuum reference, each written Silo frame additionally contains <computeroutput>&lt;field&gt;Exact</computeroutput> and <computeroutput>&lt;field&gt;Error</computeroutput>, where the signed error is numerical minus exact. For Sod, these include density, pressure, total gas energy, velocity, and momentum; for gravity, potential and every acceleration component; for streaming, radiation energy and every flux component. These fields can be overlaid or plotted directly in VisIt. Hydro output also includes the numerical velocity.</para>
<para><computeroutput>analytic-errors.json</computeroutput> in the output directory contains the latest written frame&apos;s time, resolution, reference status, relative <computeroutput>L1</computeroutput>, <computeroutput>L2</computeroutput>, and <computeroutput>Linf</computeroutput> norms, and <computeroutput>absoluteL1</computeroutput>, <computeroutput>absoluteL2</computeroutput>, <computeroutput>absoluteLinf</computeroutput>, and reference norms in each field&apos;s stated CGS units. Schema version 3 retains dimensionless <computeroutput>L1</computeroutput>, <computeroutput>L2</computeroutput>, and <computeroutput>Linf</computeroutput> and adds reference kind, population/source counts, selected global target indices, RNG/seed and sampling uncertainty estimates. <computeroutput>cells</computeroutput> is the number of compared targets; <computeroutput>totalCells</computeroutput> is the population size. The final frame is always written when output is enabled. With <computeroutput>output.enabled=off</computeroutput>, the console comparison still runs and any accuracy gate still applies; no output files are created.</para>
<para>References are evaluated at <bold>cell centers</bold>, at the actual synchronized snapshot time, including nonzero times for Sod and streaming. Let <computeroutput>e_i</computeroutput> be the numerical value minus its exact point value and <computeroutput>V_i</computeroutput> the cell measure. Then</para>
<para><itemizedlist>
<listitem><para><computeroutput>L1 = sum(V_i*abs(e_i)) / sum(V_i*abs(f_i))</computeroutput>;</para>
</listitem><listitem><para><computeroutput>L2 = sqrt(sum(V_i*e_i^2) / sum(V_i*f_i^2))</computeroutput>;</para>
</listitem><listitem><para><computeroutput>Linf = max(abs(e_i)) / max(abs(f_i))</computeroutput>.</para>
</listitem></itemizedlist>
</para>
<para>Here <computeroutput>f_i</computeroutput> is the analytic field value. Each denominator uses the same norm as the numerator, and the volume measure cancels where appropriate. If the analytic field is identically zero, a relative norm is undefined; JSON uses <computeroutput>null</computeroutput> and the console reports the absolute norms with CGS units instead. The numerical hydro fields are finite-volume values; these comparisons use exact point samples, <bold>not exact volume averages</bold>. Discontinuities therefore have sampling error, and Linf need not decrease monotonically as they move relative to cell centers. The convergence tests check L1 reduction.</para>
</sect1>
<sect1 id="md_docs_2analytic-verification_1autotoc_md10">
<title>Direct gravity reference (default)</title>
<para><computeroutput>verification.gravityReference=direct</computeroutput> applies to every gravity-enabled build. The reference uses the current snapshot density, cell mass <computeroutput>rho*dx^3</computeroutput>, isolated <computeroutput>-G*m/r</computeroutput> potential and its acceleration, with the same self-interaction exclusion as the solver. Every selected target is summed against <bold>all nonzero sources</bold>; only the targets are sampled. Vacuum target cells remain in the population. No continuum density approximation or FMM operator enters the reference sum. CGS contributions are accumulated in <computeroutput>long double</computeroutput> to reduce summation roundoff.</para>
<para><table rows="6" cols="3"><row>
<entry thead="yes"><para>Option   </para>
</entry><entry thead="yes"><para>Default   </para>
</entry><entry thead="yes"><para>Meaning    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>gravity.multipoleOrder</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>5</computeroutput>   </para>
</entry><entry thead="no"><para>Integer expansion order from 1 through 10.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>verification.gravityReference</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>direct</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>direct</computeroutput> or <computeroutput>continuum</computeroutput>. The latter retains the smooth problem-provided reference.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>verification.directSamples</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>0</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>0</computeroutput>: automatic; positive: requested target count, clamped to the number of cells and overriding the automatic pair budget. Set it at least as large as the population to force a full comparison.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>verification.directMaxPairs</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>20000000</computeroutput>   </para>
</entry><entry thead="no"><para>Automatic work budget. Compare every target if <computeroutput>Ntargets*Nsources</computeroutput> fits; otherwise sample <computeroutput>min(1024, max(1, budget/Nsources))</computeroutput> targets. At least one complete target sum is always done.    </para>
</entry></row>
<row>
<entry thead="no"><para><computeroutput>randomSeed</computeroutput>   </para>
</entry><entry thead="no"><para><computeroutput>5489</computeroutput>   </para>
</entry><entry thead="no"><para>Global nonnegative 64-bit signed integer seed (0 through 9223372036854775807).   </para>
</entry></row>
</table>
</para>
<para>For example, append these to the usual sphere run:</para>
<para><programlisting filename=".bash"><codeline><highlight class="normal">--gravity.multipoleOrder=10<sp/>--verification.directSamples=2048<sp/>--randomSeed=5489</highlight></codeline>
</programlisting></para>
<para>The generator is <computeroutput>std::mt19937_64</computeroutput>. Floyd sampling without replacement uses an explicit rejection/remainder mapping, avoiding library-dependent distribution algorithms. Targets are identified by global x-fast index <computeroutput>(z*N+y)*N+x</computeroutput> and sources are summed in that same canonical order. For a fixed mesh, target count and seed, changing order, opening angle, block order or HPX scheduling preserves the sample. Explicit sample counts also keep the same targets as density evolves. Automatic counts may change if the number of nonzero sources changes.</para>
<para>Console and JSON norms use only the selected targets. On the current uniform mesh, every target has the same volume weight. For a sampled comparison, the console reports approximate 95% half-widths for relative L1 and L2, and warns when either exceeds the corresponding measured error. A one-target sample or zero sample reference norm produces an unavailable-uncertainty warning.</para>
<para>The uncertainty estimate uses a ratio delta method, including the covariance of error and reference magnitudes and the finite-population correction. With <computeroutput>k</computeroutput> sampled targets out of <computeroutput>N</computeroutput>, for L1 let <computeroutput>a_i=abs(error_i)</computeroutput>, <computeroutput>b_i=abs(ref_i)</computeroutput>, <computeroutput>R=mean(a)/mean(b)</computeroutput>, and <computeroutput>z_i=(a_i-R*b_i)/mean(b)</computeroutput>. The estimated half-width is <computeroutput>1.96*sqrt((1-k/N)*sum(z_i^2)/(k*(k-1)))</computeroutput>. L2 applies the same estimator to squared errors and reference values, then divides the half-width for their ratio by twice the relative L2 norm. A full census has zero sampling uncertainty. These are approximate normal intervals, not guarantees: a small sample can miss rare large errors, and zero observed errors cannot exclude unobserved errors. Linf is explicitly the <bold>sample maximum</bold>, with no full-population confidence bound. Accuracy gates likewise apply to the sampled norms.</para>
<para>Direct comparisons populate the console/JSON report; they do not create dense <computeroutput>Exact</computeroutput>/<computeroutput>Error</computeroutput> Silo arrays, since unsampled cells have no direct reference. The usual numerical gravity Silo fields remain available. Select <computeroutput>verification.gravityReference=continuum</computeroutput> to obtain the original dense analytic Silo fields and to measure density-discretization plus FMM error against the smooth sphere/Gaussian solution. Direct mode isolates the FMM error for the actual cell-center point masses.</para>
</sect1>
<sect1 id="md_docs_2analytic-verification_1autotoc_md11">
<title>Automated checks</title>
<para><computeroutput>analytic.references</computeroutput> independently checks:</para>
<para><itemizedlist>
<listitem><para>Sod star-state values and shock jump conditions at a second gamma;</para>
</listitem><listitem><para>spherical potentials and forces against shell integration, including the Gaussian center, small radii, cutoff, and exterior;</para>
</listitem><listitem><para>streaming wraparound with reduced light speed;</para>
</listitem><listitem><para>zero-error synthetic fields, deliberately injected errors, availability, disabled mode, and invalid Sod reference times.</para>
</listitem></itemizedlist>
</para>
<para><computeroutput>analytic.convergence</computeroutput> runs each supported compiled problem at two resolutions, checks every nonzero reference field&apos;s relative L1 decrease, and requires a bounded final error. Gravity uses 16 and 32 cells per axis and opening angle 0.35; 3D transport uses 16 and 32, other transport builds use 32 and 64. Silo tests read back exact/error arrays, their units, and multivar registration. Application tests parse the JSON report and check nonzero exit on failed gates. HPX serialization tests cover the verification options and global seed. <computeroutput>gravity.directReference</computeroutput> checks point-mass fields and self exclusion, vacuum, explicit/automatic sampling, reproducibility and block-order independence, plus the uncertainty estimator and warnings. The <computeroutput>gravity</computeroutput> check compares all orders 1 through 10 against an independent full direct sum. The existing mesh convergence test explicitly uses continuum gravity.</para>
</sect1>
<sect1 id="md_docs_2analytic-verification_1autotoc_md12">
<title>Adding a problem</title>
<para>Implement <computeroutput>problemReference</computeroutput> in that problem&apos;s <computeroutput>problem.cpp</computeroutput>. Return a named <computeroutput>verification::Reference</computeroutput> with an evaluator returning typed CGS <computeroutput>ExactState</computeroutput> values for all enabled physics. Give any time restriction in <computeroutput>validUntil</computeroutput> and explain it in <computeroutput>reason</computeroutput>. Return an empty evaluator and a useful reason when no analytic result is available. The common reporting and Silo paths then work without changes. Add independent reference identities and a problem-appropriate resolution test to <computeroutput>tests/analyticChecks.cpp</computeroutput> and register it in CMake.</para>
<para>Sod&apos;s reference matches the rarefaction and shock curves using a bracketed pressure solve, then evaluates the self-similar regions. It is independent of the evolution code&apos;s approximate HLLC solver. For the derivation, see <ulink url="https://www.clawpack.org/riemann_book/html/Euler.html">Ketcheson, LeVeque, and del Razo, Euler equations</ulink>. Spherical gravity follows the Newtonian shell theorem, including outer shells in the potential and enclosed mass in the force. </para>
</sect1>
    </detaileddescription>
    <location file="docs/analytic-verification.md"/>
  </compounddef>
</doxygen>
