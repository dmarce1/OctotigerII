add_executable(numericalChecks numericalChecks.cpp)
target_link_libraries(numericalChecks PRIVATE OctotigerII::Subgrid)
add_test(NAME transport COMMAND numericalChecks transport)
add_test(NAME gravity COMMAND numericalChecks gravity)
if(OCTOTIGERII_WITH_SILO)
  add_executable(siloChecks siloChecks.cpp)
  target_link_libraries(siloChecks PRIVATE OctotigerII::Application ${Silo_LIBRARY})
  target_include_directories(siloChecks PRIVATE ${Silo_INCLUDE_DIR})
  add_test(NAME silo COMMAND siloChecks ${CMAKE_CURRENT_BINARY_DIR}/silo-output)
endif()
set(testRuntimeArgs)
if(OCTOTIGERII_WITH_HPX)
  set(testRuntimeArgs --hpx:threads=2 --hpx:bind=none)
endif()
foreach(problem sod kelvin-helmholtz gravity-sphere gravity-gaussian streaming radiation-pulse collapse)
  add_test(NAME example.${problem} COMMAND octotigerII
    --config=${PROJECT_SOURCE_DIR}/examples/${problem}.ini --output.enabled=off ${testRuntimeArgs})
  set_tests_properties(example.${problem} PROPERTIES TIMEOUT 120)
endforeach()
find_package(Python3 QUIET COMPONENTS Interpreter)
if(Python3_Interpreter_FOUND)
  add_test(NAME application COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/application.py
    $<TARGET_FILE:octotigerII> ${PROJECT_SOURCE_DIR}/examples ${CMAKE_CURRENT_BINARY_DIR}/application-output ${testRuntimeArgs})
endif()
