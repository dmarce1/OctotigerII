# Use an installed GoogleTest first (including module/package-manager prefixes).
# The pinned fallback requires no root access. Offline users can disable fetching
# or supply FETCHCONTENT_SOURCE_DIR_GOOGLETEST to an unpacked source tree.
option(OCTOTIGERII_FETCH_GOOGLETEST "Fetch GoogleTest if no installation is found" ON)
find_package(GTest 1.12 QUIET)
if(NOT GTest_FOUND)
  if(NOT OCTOTIGERII_FETCH_GOOGLETEST)
    message(FATAL_ERROR "GoogleTest 1.12+ is required for tests. Set GTest_ROOT/CMAKE_PREFIX_PATH, enable OCTOTIGERII_FETCH_GOOGLETEST, or disable OCTOTIGERII_BUILD_TESTS.")
  endif()
  include(FetchContent)
  if(POLICY CMP0135)
    cmake_policy(SET CMP0135 NEW)
  endif()
  set(INSTALL_GTEST OFF CACHE BOOL "" FORCE)
  set(BUILD_GMOCK OFF CACHE BOOL "" FORCE)
  FetchContent_Declare(googletest
    URL https://github.com/google/googletest/archive/refs/tags/v1.14.0.tar.gz
    URL_HASH SHA256=8ad598c73ad796e0d8280b082cebd82a630d73e73cd3c70057938a6501bba5d7)
  FetchContent_MakeAvailable(googletest)
endif()
include(GoogleTest)

set(testRuntimeArgs)
if(OCTOTIGERII_WITH_HPX)
  set(testRuntimeArgs --hpx:threads=2 --hpx:bind=none)
endif()
set(defaultProblem "")
if(OCTOTIGERII_HYDRO)
  set(defaultProblem sod)
elseif(OCTOTIGERII_RADIATION)
  set(defaultProblem streaming)
elseif(OCTOTIGERII_GRAVITY)
  set(defaultProblem gravity-sphere)
endif()

function(octo_test name module labels)
  set(target ${name}-${suffix})
  set(problem "${defaultProblem}")
  if(ARGC GREATER 3)
    set(problem "${ARGV3}")
  endif()
  if(name STREQUAL "gravityParallelChecks")
    add_executable(${target} ${name}.cpp)
  else()
    add_executable(${target} ${name}.cpp googleTestMain.cpp)
  endif()
  target_link_libraries(${target} PRIVATE OctotigerII${suffix}::${module} GTest::gtest)
  target_compile_definitions(${target} PRIVATE OCTOII_TEST_PROBLEM="${problem}")
  if(OCTOTIGERII_WITH_HPX AND module MATCHES "^(Subgrid|Gravity)$")
    target_compile_definitions(${target} PRIVATE OCTOTIGERII_WITH_HPX=1)
    target_link_libraries(${target} PRIVATE HPX::hpx)
  endif()
  gtest_discover_tests(${target} TEST_PREFIX "${suffix}.${name}."
    EXTRA_ARGS ${testRuntimeArgs} DISCOVERY_MODE PRE_TEST
    PROPERTIES TIMEOUT 300 LABELS "${labels}")
  if(OCTOTIGERII_WITH_HPX AND module STREQUAL "Application" AND Python3_Interpreter_FOUND)
    foreach(count 2 3)
      if(name STREQUAL "gravityTimeIntegrationChecks")
        # Keep each distributed case below the launcher's per-process timeout.
        # Fixed-mesh temporal convergence is checked by the serial/single-locality cases.
        foreach(case
            HierarchicalSubcyclesAndConserves
            ConventionalSubcyclesAndConserves
            PeriodicAndReflectingShellWorkConserves
            HierarchicalForcesBalanceMomentumAfterSubcycling
            RegridAfterCoupledSubcyclesClosesAllBudgets
            InvalidIntervalPreservesPublishedState
            DisabledTimeRefinementMatchesGlobalComposition
            NaiveSubcyclesAndRegridEnergyOptionRemainsIndependent
            NestedThreeLevelShellAndFluxRegistersConserve
            FineCflCanRequireMoreThanTwoSubsteps)
          add_test(NAME distributed.${target}.${case}.${count} COMMAND ${Python3_EXECUTABLE}
            ${CMAKE_CURRENT_SOURCE_DIR}/distributed.py $<TARGET_FILE:${target}>)
          set_tests_properties(distributed.${target}.${case}.${count} PROPERTIES
            ENVIRONMENT "OCTOTIGERII_TEST_LOCALITIES=${count};GTEST_FILTER=GravityTimeIntegration.${case}"
            TIMEOUT 360 LABELS "distributed" RUN_SERIAL TRUE)
        endforeach()
        continue()
      endif()
      add_test(NAME distributed.${target}.${count} COMMAND ${Python3_EXECUTABLE}
        ${CMAKE_CURRENT_SOURCE_DIR}/distributed.py $<TARGET_FILE:${target}>)
      set_tests_properties(distributed.${target}.${count} PROPERTIES
        ENVIRONMENT "OCTOTIGERII_TEST_LOCALITIES=${count}" TIMEOUT 360 LABELS "distributed" RUN_SERIAL TRUE)
    endforeach()
  endif()
endfunction()
find_package(Python3 QUIET COMPONENTS Interpreter)
octo_test(meshChecks Mesh unit)
octo_test(mathChecks Mesh unit)
octo_test(limiterChecks Physics unit)
octo_test(laneEmdenChecks Subgrid "unit;polytrope")
if(OCTOTIGERII_HYDRO)
  octo_test(hydroChecks Hydro unit)
  octo_test(dualEnergyChecks Application dual-energy)
  octo_test(timeRefinementChecks Application "integration;amr;time-refinement")
  if(OCTOTIGERII_MASS_FRACTIONS)
    octo_test(speciesChecks Application "integration;species")
  endif()
  if(OCTOTIGERII_GRAVITY AND OCTOTIGERII_MASS_FRACTIONS)
    octo_test(gravityEnergyChecks Application "integration;conservation;gravity")
    octo_test(gravityTimeIntegrationChecks Application "integration;conservation;gravity;time-refinement")
    octo_test(gravityFluxWorkChecks Application "unit;gravity")
  endif()
endif()
if(OCTOTIGERII_RADIATION)
  octo_test(radiationChecks Radiation unit)
endif()
if(OCTOTIGERII_HYDRO OR OCTOTIGERII_RADIATION)
  octo_test(finiteVolumeChecks Application integration)
endif()
if(OCTOTIGERII_GRAVITY)
  octo_test(gravityChecks Gravity unit)
  octo_test(ewaldChecks Gravity "unit;gravity")
  octo_test(directGravityChecks Application integration gravity-sphere)
  octo_test(gravityParallelChecks Application integration gravity-sphere)
  octo_test(partialGravityChecks Application "integration;gravity;time-refinement" gravity-sphere)
endif()
if(defaultProblem)
  octo_test(unitsChecks Subgrid unit)
  octo_test(optionsChecks Subgrid unit)
  octo_test(boundaryChecks Application "unit;integration")
  octo_test(numericalChecks Application integration)
  if(OCTOTIGERII_RADIATION AND OCTOTIGERII_NDIM EQUAL 1)
    target_compile_definitions(numericalChecks-${suffix} PRIVATE OCTOTIGERII_TEST_STREAMING_PROFILE=1)
  endif()
  octo_test(storageChecks Application integration)
  octo_test(amrChecks Application "unit;integration;amr")
  octo_test(conservationChecks Application "integration;conservation")
  octo_test(analyticChecks Application integration)
  octo_test(siloChecks Application integration)
  target_link_libraries(siloChecks-${suffix} PRIVATE ${Silo_LIBRARY})
  target_include_directories(siloChecks-${suffix} PRIVATE ${Silo_INCLUDE_DIR})
  if(OCTOTIGERII_WITH_HPX)
    octo_test(serializationChecks Application unit)
  endif()
endif()
if(OCTOTIGERII_HYDRO AND OCTOTIGERII_NDIM EQUAL 3)
  octo_test(rayleighTaylorChecks Application "integration;rayleigh-taylor" rayleigh-taylor)
  if(OCTOTIGERII_GRAVITY)
    octo_test(polytropeChecks Application "integration;polytrope" polytrope)
  endif()
endif()
if(Python3_Interpreter_FOUND)
  add_test(NAME application.${suffix} COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/application.py
    $<TARGET_FILE:octoII-${suffix}> ${PROJECT_SOURCE_DIR} ${OCTOTIGERII_NDIM}
    ${OCTOII_WITH_HYDRO} ${OCTOII_WITH_RADIATION} ${OCTOII_WITH_GRAVITY} ${testRuntimeArgs})
  set_tests_properties(application.${suffix} PROPERTIES TIMEOUT 300 LABELS application)
endif()
